import { Paper } from '@mantine/core'

export default function HomePage() {
  return (
    <Paper
      sx={{
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      Home Page
    </Paper>
  )
}
